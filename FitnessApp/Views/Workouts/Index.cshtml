@model List<Workout>

@{
    ViewData["Title"] = "Мои тренировки";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <h2 class="mb-4">Мои тренировки</h2>

    <p>
        <a href="/Workouts/Create" class="btn btn-primary">Добавить тренировку</a>
    </p>

    @if (Model.Any())
    {
        <table class="table table-bordered">
            <thead class="table-primary">
                <tr>
                    <th>Дата</th>
                    <th>Название</th>
                    <th>Продолжительность (мин)</th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var workout in Model)
                {
                    <tr>
                        <td>@workout.Date.ToString("dd.MM.yyyy")</td>
                        <td>@workout.Name</td>
                        <td>@workout.Duration</td>
                        <td>
                            <a href="/Workouts/Edit/@workout.Id" class="btn btn-warning btn-sm">Редактировать</a>
                            <form method="post" action="/Workouts/Delete" style="display:inline;">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@workout.Id" />
                                <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Удалить тренировку?');">Удалить</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>У вас пока нет тренировок.</p>
    }
</div>